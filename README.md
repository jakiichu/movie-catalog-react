# Запуск React-приложения на Vite

Этот документ описывает, как установить зависимости и запустить React‑приложение, созданное с помощью **Vite**.

## 📦 Требования

Перед началом убедитесь, что у вас установлены:

* **Node.js** версии **20.x** или выше
  Проверить версию:

  ```bash
  node -v
  ```
* **npm** (обычно устанавливается вместе с Node.js) или **yarn / pnpm**

Рекомендуется использовать последнюю LTS‑версию Node.js.

## 🚀 Установка и запуск

### 1. Клонирование репозитория

```bash
git clone https://github.com/jakiichu/movie-catalog-react
cd only
```

### 2. Установка зависимостей

С помощью **npm**:

```bash
npm install
```

Или с помощью **yarn**:

```bash
yarn
```

Или **pnpm**:

```bash
pnpm install
```

### 3. Запуск приложения в режиме разработки

```bash
npm run dev
```

После запуска в терминале появится адрес, обычно:

```
http://localhost:5173
```

Откройте его в браузере — приложение готово к работе 🎉

## 🛠 Доступные скрипты

* `npm run dev` — запуск в режиме разработки
* `npm run build` — сборка проекта для продакшена
* `npm run preview` — локальный просмотр продакшн‑сборки
* `npm run lint` — проверка кода (если настроен ESLint)

## 📁 Структура проекта

### Дерево каталога `src`

```
├── app
│   ├── common
│   │   ├── const          // Глобальные константы приложения (ключи, дефолтные значения)
│   │   ├── enum           // Перечисления (enum), используемые в разных модулях
│   │   └── styles         // Общие стили, темы, переменные дизайна
│   │
│   ├── module
│   │   └── movies         // Функциональный модуль "Фильмы"
│   │       ├── case       // Прикладная логика (use cases на уровне UI)
│   │       │   ├── create // Сценарий создания фильма
│   │       │   │   └── case
│   │       │   ├── delete // Сценарий удаления фильма
│   │       │   │   └── case
│   │       │   ├── edit   // Сценарий редактирования фильма
│   │       │   │   ├── case
│   │       │   │   └── component
│   │       │   │       └── data-wrapper // Обёртки данных для UI
│   │       │   ├── get-one // Получение одного фильма
│   │       │   │   └── case
│   │       │   └── list   // Получение списка фильмов
│   │       │       ├── case
│   │       │       └── component
│   │       │           ├── add-movies   // UI для добавления фильмов
│   │       │           ├── filter       // UI-фильтрация списка
│   │       │           ├── list         // Компоненты списка
│   │       │           │   └── component
│   │       │           │       ├── actions       // Кнопки действий (edit/delete)
│   │       │           │       └── no-have-item  // Заглушка, если список пуст
│   │       │           └── search       // Поиск по фильмам
│   │       │
│   │       └── form       // Формы, связанные с фильмами
│   │
│   ├── router             // Маршрутизация приложения (экраны, переходы)
│   │
│   ├── ui
│   │   ├── autocomplete   // Переиспользуемые UI-компоненты
│   │   ├── button
│   │   ├── common
│   │   ├── date-picker
│   │   ├── dim-wrapper
│   │   ├── icon-button
│   │   ├── input
│   │   ├── modal
│   │   ├── multiInput
│   │   └── popover
│   │                     // UI-kit: атомарные, независимые компоненты
│   │
│   └── utils              // Вспомогательные функции (форматирование, хелперы)
│
├── data
│   ├── db                 // Работа с источником данных (БД, local storage и т.п.)
│   ├── repository         // Репозитории — абстракция доступа к данным
│   └── singletone         // Singleton-объекты (конфигурации, клиенты)
│
└── domain
    ├── common
    │   └── http           // Общая HTTP-логика (клиенты, запросы, ответы)
    │
    └── movies
        ├── interface      // Интерфейсы и контракты домена "Фильмы"
        └── use-case       // Бизнес-логика (чистые use cases, без UI)

```

project-root/
├── public/ # Статические файлы
├── src/ # Исходный код
│ ├── assets/ # Изображения, стили и т.п.
│ ├── components/ # React‑компоненты
│ ├── App.jsx # Корневой компонент
│ └── main.jsx # Точка входа
├── index.html # HTML‑шаблон
├── package.json # Зависимости и скрипты
└── vite.config.js # Конфигурация Vite

````

## ⚙️ Сборка проекта

Для сборки оптимизированной версии приложения выполните:

```bash
npm run build
````

Результат будет сохранён в папке `dist/`.

## ❗ Возможные проблемы

* **Ошибка версии Node.js** — обновите Node.js до LTS‑версии
* **Порт занят** — Vite автоматически предложит другой порт
* **Зависимости не устанавливаются** — попробуйте удалить `node_modules` и `package-lock.json`, затем выполнить
  `npm install`

## 📚 Полезные ссылки

* Vite: [https://vitejs.dev/](https://vitejs.dev/)
* React: [https://react.dev/](https://react.dev/)

## Коментарий от разработчика

Здравствуйте проверяющий компании Intrice!

Хочу немного прояснить, почему для тестового задания я выбрал React, а не Angular.

Моя цель — стать разработчиком, который не зависит от конкретного стека технологий. Однако для уверенной и качественной
работы с разными фреймворками требуется достаточно много времени, которым я, к сожалению, сейчас не располагаю. Если бы
у меня была хотя бы свободная неделя, я вполне мог бы реализовать это задание на Angular.

При этом, если посмотреть на итоговое решение, можно заметить, что используемый мной подход во многом близок к Angular.
Я активно применяю ООП, структура компонентов выстроена схожим образом: отдельные TypeScript-файлы, SCSS-стили,
разметка, а также продуманный архитектурный подход.

С концепцией чистой архитектуры я впервые познакомился благодаря своему наставнику, для которого Angular был первым
фреймворком. Насколько я понимаю, архитектурные решения и грамотная структура приложений в Angular сегодня достаточно
востребованы, и этот опыт я стараюсь применять независимо от выбранного фреймворка.

Также хочу отметить, что в процессе работы в отдельных местах я мог использовать нейронные сети как вспомогательный
инструмент. Сроки были достаточно сжаты, и, вместо того чтобы тратить время на поиск или повторное написание решений,
которые я уже реализовывал много раз ранее, я использовал ИИ для ускорения отдельных процессов. При этом основное
понимание, архитектура и реализация решения — это результат моей работы. В общей сложности на выполнение данного задания
я потратил более 26 часов.

Надеюсь, это пояснение поможет лучше понять мой подход к выполнению тестового задания.

Для дополнительного задания я выбрал дополнить фичу просмотра всех. Дополнительно там есть: debounce, loading, состояние
отсутствия элементов, и так же накидал светлую и темную тему.
